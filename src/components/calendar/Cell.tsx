import clsx from "clsx";
import { pointToString } from "../../lib/utils";
import { Children, ReactNode } from "react";
import React from "react";
import { CalendarMonthTable } from "../../lib/CalendarBuilder";

type CellProps = {};

export const Cell = ({}: CellProps) => {
  return null;
};
